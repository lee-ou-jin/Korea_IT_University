"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResourceInfo = void 0;
class ResourceInfo {
    constructor(client, knex, databaseName, tableName, properties) {
        this.client = client;
        this.knex = knex;
        this.databaseName = databaseName;
        this.tableName = tableName;
        this.properties = properties;
        const idProperty = properties.find((p) => p.isId());
        if (!idProperty) {
            throw new Error(`table ${tableName} has no primary key`);
        }
        this.idProperty = idProperty;
    }
}
exports.ResourceInfo = ResourceInfo;
//# sourceMappingURL=ResourceInfo.js.map