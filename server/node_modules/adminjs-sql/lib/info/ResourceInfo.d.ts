import { Knex } from 'knex';
import { Property } from '../adapter/Property';
import { Client } from '../typing/Client';
export declare class ResourceInfo {
    client: Client;
    readonly knex: Knex;
    readonly databaseName: string;
    readonly tableName: string;
    readonly properties: Property[];
    idProperty: Property;
    constructor(client: Client, knex: Knex, databaseName: string, tableName: string, properties: Property[]);
}
